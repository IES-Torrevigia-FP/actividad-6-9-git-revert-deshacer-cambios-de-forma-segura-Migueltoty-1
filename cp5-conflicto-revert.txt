En la rama main
Tu rama está adelantada a 'origin/main' por 10 commits.
  (usa "git push" para publicar tus commits locales)

Estás revirtiendo el commit ccc05f0.
  (todos los conflictos corregidos: ejecute «git revert --continue»)
  (usa "git revert --skip" para omitir este parche)
  (usa "git revert --abort" para cancelar la operación de revertir)

Archivos sin seguimiento:
  (usa "git add <archivo>..." para incluirlo a lo que será confirmado)
	cp5-conflicto-revert.txt

no hay nada agregado al commit pero hay archivos sin seguimiento presentes (usa "git add" para hacerles seguimiento)

borramos los problemas del conflicto dentro de config.txt
y continuamos el revert
